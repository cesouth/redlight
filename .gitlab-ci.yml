# Lint + tests across supported Python versions (mirror of the former
# GitHub Actions matrix). Pipelines run on branch pushes and merge requests,
# without duplicating both for the same commit.

workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH && $CI_OPEN_MERGE_REQUESTS
      when: never
    - if: $CI_COMMIT_BRANCH

stages:
  - test

test:
  stage: test
  image: python:${PYTHON_VERSION}
  parallel:
    matrix:
      - PYTHON_VERSION: ["3.9", "3.11", "3.13"]
  variables:
    PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  cache:
    key: "pip-py${PYTHON_VERSION}"
    paths:
      - .cache/pip
  script:
    # shapefile extra (pyogrio) needs Python 3.10+; the 3.9 leg installs
    # without it and the pyogrio-dependent tests self-skip via
    # pytest.importorskip.
    - if [ "$PYTHON_VERSION" = "3.9" ]; then pip install -e .[dev,mapping]; else pip install -e .[dev,mapping,shapefile]; fi
    - ruff check src tests
    - pytest -q
